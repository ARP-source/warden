# Kept for tooling that expects it. Vercel installs from pyproject.toml when one
# is present and ignores this file, so pyproject is the source of truth for the
# deployed function.
fastapi>=0.115
pydantic>=2.9
httpx>=0.27
weave>=0.51
